# ========================================
# CipherVault - Aplicação Autónoma
# ========================================
# Instalar com: pip install -r requirements.txt
# Depois a aplicação funciona diretamente!

# Criptografia (OBRIGATÓRIO) - Inclui OpenSSL internamente
cryptography==42.0.0

# Hashing de palavras-passe seguro (para autenticação)
argon2-cffi==23.1.0

# Interface de utilizador (CLI melhorada)
click==8.1.7          # Para interface de linha de comandos profissional
colorama==0.4.6       # Cores no terminal (compatível com Windows)
rich==13.7.0          # Interface CLI bonita e moderna

# Barra de progresso
tqdm==4.66.1          # Para mostrar progresso da cifragem

# ========================================
# Dependências opcionais
# ========================================

# GUI (Interface gráfica) - Descomentar se necessário
# PyQt6==6.6.1
# ou tkinter (já incluído no Python)

# Base de dados (se gestão de utilizadores)
# python-dotenv==1.0.0  # Variáveis de ambiente
# pydantic==2.5.3       # Validação de dados

# ========================================
# Total: ~5 MB de dependências
# Aplicação 100% autónoma após instalação!
# ========================================
